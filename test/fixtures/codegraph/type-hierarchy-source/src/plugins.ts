export interface Plugin {
  run(): void;
}

export class AlphaPlugin implements Plugin {
  run(): void {}
}

export class BravoPlugin implements Plugin {
  run(): void {}
}

export class CharliePlugin implements Plugin {
  run(): void {}
}

export class DeltaPlugin implements Plugin {
  run(): void {}
}

export class EchoPlugin implements Plugin {
  run(): void {}
}

export class FoxtrotPlugin implements Plugin {
  run(): void {}
}

export class GolfPlugin implements Plugin {
  run(): void {}
}

export class HotelPlugin implements Plugin {
  run(): void {}
}

export class IndiaPlugin implements Plugin {
  run(): void {}
}
